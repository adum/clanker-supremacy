return {
  overlay = {
    enabled = true,
    update_interval_ticks = 15,
    top_margin = 12,
    left_margin = 20,
    right_margin = 20,
    inventory_top_offset = 40,
    inventory_width = 260,
    max_inventory_lines = 24
  },
  map_marker = {
    enabled = true,
    update_interval_ticks = 60,
    chart_radius = 16,
    text = "Builder",
    refresh_distance = 1
  }
}
